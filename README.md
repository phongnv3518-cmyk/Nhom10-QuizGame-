# Project README

This project uses a Python virtual environment located in `.venv`.

Activation (PowerShell):

```powershell
. .\.venv\Scripts\Activate.ps1
```

Run the simple server:

```powershell
python server.py
```

Run the simple client (in another shell):

```powershell
python client.py
```
